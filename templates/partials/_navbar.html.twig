<div>
  <h3 class="float-md-start mb-0">Mini-Chat</h3>
  <nav class="nav nav-masthead justify-content-center float-md-end">
    <a class="nav-link fw-bold py-1 px-0{{ app.request.attributes.get('_route') == 'home' ? ' active' }}" href="{{ path('home') }}">Accueil</a>
    <a class="nav-link fw-bold py-1 px-0{{ app.request.attributes.get('_route') == 'app_register' ? ' active' }}" href="{{ path('app_register') }}">Inscription</a>
    <a class="nav-link fw-bold py-1 px-0{{ app.request.attributes.get('_route') == 'app_login' ? ' active' }}" href="{{ path('app_login') }}">Connexion</a>
  </nav>
</div>